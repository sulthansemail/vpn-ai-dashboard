import { ArrowUpRight, MonitorSmartphone } from "lucide-react";
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
import { SubscriptionBadge } from "@/components/customers/SubscriptionBadge";
import { Table, TableBody, TableCaption, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import type { Customer } from "@/mock/customers";
interface CustomerTableProps { customers: Customer[]; onOpenCustomer: (customer: Customer) => void; }
export function CustomerTable({ customers, onOpenCustomer }: CustomerTableProps) { return <Table><TableCaption className="sr-only">Customer operations roster</TableCaption><TableHeader><TableRow className="border-white/8 hover:bg-transparent">{["Customer", "Connection", "Subscription", "Last active", "Devices", "Value", ""].map((heading) => <TableHead key={heading} className="h-12 px-4 text-xs font-medium text-slate-500">{heading}</TableHead>)}</TableRow></TableHeader><TableBody>{customers.map((customer) => <TableRow key={customer.id} className="border-white/8 hover:bg-white/[0.03]"><TableCell className="px-4"><div className="flex items-center gap-3"><Avatar><AvatarFallback className="bg-blue-500/15 text-xs font-semibold text-blue-300">{customer.initials}</AvatarFallback></Avatar><div><p className="font-medium text-slate-100">{customer.name}</p><p className="text-xs text-slate-500">{customer.email} · {customer.country}</p></div></div></TableCell><TableCell className="px-4"><p className="text-sm text-slate-200">{customer.currentServer}</p><p className="text-xs text-slate-500">{customer.platform}</p></TableCell><TableCell className="px-4"><SubscriptionBadge status={customer.subscription} /><p className="mt-1 text-xs text-slate-500">{customer.plan}</p></TableCell><TableCell className="px-4 text-sm text-slate-400">{customer.lastActive}</TableCell><TableCell className="px-4"><span className="flex items-center gap-1.5 text-slate-300"><MonitorSmartphone className="size-3.5 text-slate-500" />{customer.devices.length}</span></TableCell><TableCell className="px-4 text-sm font-medium text-slate-200">{customer.lifetimeRevenue}</TableCell><TableCell className="px-4"><Button variant="ghost" size="icon-sm" aria-label={`Open ${customer.name}`} onClick={() => onOpenCustomer(customer)}><ArrowUpRight /></Button></TableCell></TableRow>)}</TableBody></Table>; }
