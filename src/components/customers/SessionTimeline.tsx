import { Circle } from "lucide-react";
import type { Session } from "@/mock/customers";

export function SessionTimeline({ sessions }: { sessions: Session[] }) { if (!sessions.length) return <EmptyState message="No recent VPN sessions." />; return <div className="space-y-4">{sessions.map((session, index) => <div key={session.id} className="relative flex gap-3">{index < sessions.length - 1 && <span className="absolute top-6 left-2 h-[calc(100%+0.5rem)] w-px bg-white/8" />}<div className={session.status === "active" ? "relative z-1 mt-1 rounded-full text-emerald-400" : "relative z-1 mt-1 rounded-full text-slate-500"}><Circle className="size-4 fill-current" /></div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><p className="text-sm font-medium text-slate-200">{session.event} · {session.server}</p><span className="text-xs text-slate-500">{session.startedAt}</span></div><p className="mt-1 text-xs text-slate-500">{session.location} · {session.duration}</p></div></div>)}</div>; }
export function EmptyState({ message }: { message: string }) { return <p className="rounded-lg border border-dashed border-white/10 px-4 py-8 text-center text-sm text-slate-500">{message}</p>; }
