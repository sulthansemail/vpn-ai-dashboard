import { CreditCard, MonitorSmartphone, ShieldCheck, UsersRound } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { customers } from "@/mock/customers";

const metrics = [{ label: "Customers monitored", value: customers.length.toString(), detail: "Across 6 markets", icon: UsersRound }, { label: "Active subscriptions", value: customers.filter((customer) => customer.subscription === "active").length.toString(), detail: "2 renew this month", icon: ShieldCheck }, { label: "Connected devices", value: customers.reduce((total, customer) => total + customer.devices.length, 0).toString(), detail: "1.5 per customer", icon: MonitorSmartphone }, { label: "At-risk payments", value: customers.filter((customer) => customer.subscription === "past_due").length.toString(), detail: "Requires follow-up", icon: CreditCard }];
export function CustomerMetrics() { return <section className="grid grid-cols-2 gap-3 xl:grid-cols-4">{metrics.map((metric) => { const Icon = metric.icon; return <Card key={metric.label} className="gap-0 border-white/8 bg-[#111b2e] py-0"><CardContent className="p-4"><div className="flex items-start justify-between"><p className="text-xs font-medium text-slate-500">{metric.label}</p><Icon className="size-4 text-blue-400" /></div><p className="mt-3 text-2xl font-semibold text-white">{metric.value}</p><p className="mt-1 text-xs text-slate-500">{metric.detail}</p></CardContent></Card>; })}</section>; }
