import { Search, SlidersHorizontal } from "lucide-react";
import { Input } from "@/components/ui/input";
import type { CustomerPlatform, SubscriptionStatus } from "@/mock/customers";
export interface CustomerFilterValue { query: string; country: string; platform: "all" | CustomerPlatform; subscription: "all" | SubscriptionStatus; }
interface CustomerFiltersProps { value: CustomerFilterValue; countries: string[]; onChange: (value: CustomerFilterValue) => void; }
const selectClass = "h-9 rounded-lg border border-white/8 bg-white/5 px-3 text-sm text-slate-300 outline-none focus:border-blue-500/60";
export function CustomerFilters({ value, countries, onChange }: CustomerFiltersProps) { return <div className="flex flex-col gap-3 lg:flex-row"><div className="relative min-w-0 flex-1"><Search className="absolute top-2.5 left-3 size-4 text-slate-500" /><Input value={value.query} onChange={(event) => onChange({ ...value, query: event.target.value })} placeholder="Search name, email, or server..." className="h-10 border-white/8 bg-white/5 pl-9 text-slate-200 placeholder:text-slate-500" /></div><div className="flex flex-wrap gap-2"><span className="flex items-center px-1 text-slate-500"><SlidersHorizontal className="size-4" /></span><select className={selectClass} aria-label="Country" value={value.country} onChange={(event) => onChange({ ...value, country: event.target.value })}><option value="all">All countries</option>{countries.map((country) => <option key={country}>{country}</option>)}</select><select className={selectClass} aria-label="Platform" value={value.platform} onChange={(event) => onChange({ ...value, platform: event.target.value as CustomerFilterValue["platform"] })}><option value="all">All platforms</option><option>iOS</option><option>Android</option><option>macOS</option><option>Windows</option><option>Linux</option></select><select className={selectClass} aria-label="Subscription" value={value.subscription} onChange={(event) => onChange({ ...value, subscription: event.target.value as CustomerFilterValue["subscription"] })}><option value="all">All subscriptions</option><option value="active">Active</option><option value="trial">Trial</option><option value="past_due">Past due</option><option value="cancelled">Cancelled</option></select></div></div>; }
