import { Card, CardContent } from "@/components/ui/card";
import { activityTimeline } from "@/mock/dashboard";

export function ActivityTimeline() { return <Card className="gap-0 border-white/8 bg-[#111b2e] py-0"><CardContent className="p-5"><div><h2 className="font-semibold text-white">Recent activity</h2><p className="mt-1 text-xs text-slate-500">Infrastructure and AI operations</p></div><div className="mt-5 space-y-5">{activityTimeline.map((event, index) => { const Icon = event.icon; return <div key={event.title} className="relative flex gap-3">{index < activityTimeline.length - 1 && <span className="absolute top-7 left-3.5 h-[calc(100%+0.25rem)] w-px bg-white/8" />}<div className={`relative z-1 grid size-7 shrink-0 place-items-center rounded-full bg-white/5 ${event.color}`}><Icon className="size-3.5" /></div><div className="min-w-0 pb-0.5"><div className="flex items-start justify-between gap-2"><p className="text-sm font-medium text-slate-200">{event.title}</p><span className="shrink-0 text-[11px] text-slate-500">{event.time}</span></div><p className="mt-1 text-xs leading-5 text-slate-500">{event.detail}</p></div></div>; })}</div></CardContent></Card>; }
