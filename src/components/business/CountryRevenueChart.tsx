import { countries } from "@/mock/business"; import { Panel } from "./BusinessHealthCard"; export function CountryRevenueChart(){return <Panel title="Revenue by country"><div className="mt-4 space-y-3">{countries.map(c=><div key={c.country} className="flex justify-between text-sm"><span className="text-slate-300">{c.country}</span><span className="text-white">${(c.revenue/1000).toFixed(1)}k</span></div>)}</div></Panel>}
